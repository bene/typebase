<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>TypeBase Status</title>

    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="border-t-8 border-pink-500 bg-gray-50 bg-gray-800 min-h-screen flex flex-col">
    <div class="max-w-5xl p-5 m-auto">
        <div class="grid grid-cols-1 gap-12">
            {{#if title}}
            <div>
                <p class="text-7xl text-pink-300 mb-4">{{ statusCode }}</p>
                <p class="text-8xl text-pink-500 font-extrabold">There are some<br/>problems.</p>
            </div>

            <hr class="my-8"/>

            <p class="text-4xl text-pink-400">{{ message }}</p>
            {{else}}
            <div>
                <p class="text-7xl text-pink-300 mb-4">{{ statusCode }}</p>
                <p class="text-8xl text-pink-500 font-extrabold">{{ message }}</p>
            </div>
            {{/if}}
        </div>
    </div>

    <div class="mx-auto mb-5">
        <p class="text-2xl text-pink-300">TypeBase {{ version }}</p>
    </div>
</body>
</html>